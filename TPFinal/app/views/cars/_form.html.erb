<head>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500&display=swap" rel="stylesheet">
</head>


  <%= form_with(model: car, local: true) do |form| %>

  <div class="error">
    <% if car.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

        <ul>
          <% car.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  </div>

    <div class="field">
      <%= form.text_field :patent, placeholder: "Patent" %>
    </div>

    <br>
    <br>

    <div class="field">
      <%= form.text_field :brand, placeholder: "Brand" %>
    </div>

    <br>
    <br>

    <div class="field">
      <%= form.text_field :model, placeholder: "Model" %>
    </div>

    <br>
    <br>

    <div class="field">
      <%= form.label :type_id %> <br>
      <%= form.collection_select(:type_id, Type.all, :id, :name ) %>
    </div>

    <br>
    <br>

    <div class="field">
      <%= form.label :fuel_id %> <br>
      <%= form.collection_select(:fuel_id, Fuel.all, :id, :name ) %>
    </div>

    <br>
    <br>

    <div class="field">
      <%= form.number_field :price_day, placeholder: "Price per day" %>
    </div>

    <br>
    <br>

    <div class="actions">
      <%= form.submit %> 
    </div>

 
<% end %>
